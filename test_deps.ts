export {
    assert,
    assertEquals,
    assertThrows,
    assertThrowsAsync
  } from "https://deno.land/std/testing/asserts.ts";

  Deno.test({
    name: "Generic",
    fn(): void {
      
    }
  })
